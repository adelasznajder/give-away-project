import React, {Component} from "react";
import {
    HashRouter,
    Route,
    Link,
    Switch,
    NavLink,
} from 'react-router-dom';

export class Login extends Component {
    render() {
        return (
           <h1>Logowańsko</h1>
        );
    }
}